/**
 * API base URL for the Laravel backend (`php artisan serve` defaults to port 8000).
 * Override per environment if needed.
 */
export const API_BASE_URL =
  window.VERSITY_API_BASE_URL || "http://127.0.0.1:8000";
